# FirstF

## Command Description:

## FirstF combines the grep and head commands of Linux. It does this so it can search for a specific pattern listed by the user within N lines of a file, with N lines being dictated by the user.

## To run FirstF, type this within the terminal: node firstf.js PATTERN FILENAME NUMBER_OF_LINES.

## Where PATTERN, FILENAME, and NUMBER_OF_LINES is dictated by the user for the pattern they're looking for, within whichever file they want, in however many number of lines they want to check.

## As stated before, FirstF combines grep + head functions from Linux.




## AI Assisted Programming Reflection:

## I asked AI to give me a code file to try out the grep command, as well as a longer file(20 lines long) to test.

## AI helped in the sense to give me example files that would allow me to greater understand the command being taught, as well as play around with said test file, without having to manually make one myself to access or scan through.

## I had to think independently when actually utilizing the code on Linux(Codio) and implementing the grep and head commands to weave together to actually access the file. I also had to take the test file from the AI Generator and input it into the terminal to create a new file with those contents and place it within the directory I wanted.

## What the AI got wrong or missed would probably be content of the file, to a small degree. I tried two different AI generators(ChatGPT and Google Gemini), on ChatGPT it only gave me example entries to put into a file myself, like "Dog, Cat, Bird, Fish", rather than something like "Alice - Role: Admin - Status: Active, ect...". Different AI will generate different example files. While Gemini looked cleaner and nicer, it also included things like "Line 01: [USER]" prior to the data, which was unecessary for my uses. Pros and cons for each generator used I guess.
